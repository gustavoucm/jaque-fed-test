import { StatusUserPipe } from './status-user.pipe';

describe('StatusUserPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusUserPipe();
    expect(pipe).toBeTruthy();
  });
});
